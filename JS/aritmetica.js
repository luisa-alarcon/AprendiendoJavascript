export function sumar (a,b){
    return a + b;
}

export function restar (a,b){
    return a - b;
}

export const aritmetica = {
    sumar,
    restar
};

export const aritmetica2 = {
    sumar,
    restar
};

/* exportar por defecto 
es cuando se manda a llamar en formato de modulo el archivo es la funcion que por default se va a ejecutar 

No se pueden exportar por default 2 veces

con let, const o funciones expresadas se exporta por default de la siguiente manera*/






